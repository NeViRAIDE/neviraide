return {
  inlay_hints = {
    enabled = true,
  },
  signature = {
    bind = false,
    floating_window = false,
    hint_enable = true,
    use_lspsaga = false,
    hint_prefix = '  ',
    hint_scheme = 'Comment',
    hi_parameter = 'LspSignatureActiveParameter',
    max_height = 1,
  },
  diagnostic = {
    signs = false,
    underline = true,
    update_in_insert = false,
    virtual_text = false,
    severity_sort = true,
  }
}
